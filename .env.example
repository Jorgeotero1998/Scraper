# Logging
# One of: error, warn, info, http, verbose, debug, silly  (default: info)
LOG_LEVEL=info

# Log output format: "text" (human-readable) or "json" (structured, for log aggregators)
LOG_FORMAT=text

# Directory where rotating log files are written  (default: logs/)
LOG_DIR=logs
